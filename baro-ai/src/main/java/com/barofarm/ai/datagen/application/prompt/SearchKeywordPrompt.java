package com.barofarm.ai.datagen.application.prompt;

// 검색 키워드 생성 프롬프트 템플릿
public final class SearchKeywordPrompt {

    private SearchKeywordPrompt() {
        // 유틸리티 클래스는 인스턴스화 방지
    }

    // 검색 키워드 생성 프롬프트 템플릿
    public static String getPromptTemplate() {
        return """
            당신은 프리미엄 신선식품 이커머스에서 쇼핑하는 사용자입니다.
            농산물, 수산물, 축산물을 검색할 때 실제 사용자가 입력할 것 같은 검색 키워드를 생성해주세요.

            [요구사항]
            1. 실제 사용자가 검색창에 입력할 것 같은 자연스러운 키워드
            2. 농산물/수산물/축산물 관련 키워드
            3. 상품명이 아닌 검색어 형태 (예: "사과", "제주 감귤", "한우", "연어" 등)
                - 꼭 예시와 동일할 필요는 없습니다.
            4. 각 키워드는 1~3단어로 구성
            5. 총 5개의 서로 다른 키워드 생성

            [예시(참고용)]
            - "사과"
            - "제주 감귤"
            - "1등급 한우"
            - "노르웨이산 연어"
            - "청송 사과"
            - "바나나"
            - "고등어"
            - "닭가슴살"

            반드시 JSON 배열 형식으로만 반환하세요. 추가 설명이나 마크다운은 포함하지 마세요.
            ["키워드1", "키워드2", "키워드3", "키워드4", "키워드5"]
            """;
    }
}
